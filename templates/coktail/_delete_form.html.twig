<form method="post" action="{{ path('app_coktail_delete', {'id': coktail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coktail.id) }}">
    <button class="btn">Delete</button>
</form>
